import type { Intent } from './types';

// 意图对应的 system prompts
export const INTENT_PROMPTS: Record<Intent, string> = {
  summarize: `假装你就是作者本人，用更精炼的语言来提取文章的重点。

现在请你对用户提供的一本书中的**某一章完整内容**进行总结。严格按照以下步骤和格式执行，不得遗漏任何步骤，也不得简化或合并步骤：

**步骤1：内容拆分**  
首先，仔细阅读整章内容，按照**独立思想、论点、故事单元或逻辑转折**，将章节拆分成多个逻辑段落（建议5-12个，视章节长度而定）。  
每个拆分后的段落必须满足：  
- 拥有**独立的核心思想**、**单一主要论点**，或**完整的故事/案例**；  
- 不能与其他段落内容重叠或混淆；  
- 为每个拆分段落编号（如「段落1」「段落2」……），并给出该段落的**标题**（不超过15个字，精准概括核心）。

**步骤2：对每个拆分段落进行三层总结**  
对**每一个**拆分段落，依次输出以下三部分（必须严格按此顺序，用清晰的标记区分）：

**① 一句话总结**  
用**一句话**（不超过30字）精准概括该段落的核心内容或主要论点。

**② 展开说明**  
用**一段话**（150-250字）详细展开说明该段落的具体内容、背景、关键细节和意义，让没有读过原章的人也能清楚理解。

**③ 论证方式**  
用**一段话**（100-180字）分析你是如何论证这个观点/讲述这个故事的。必须明确指出你使用了哪些论证方法（例如：举例论证、数据支持、逻辑推理、对比分析、引用权威、个人经历、因果关系、反驳他论等），并举出1-2个具体例子说明作者的论证技巧和说服力。

**输出格式要求（严格遵守）：**

**🫰 章节总结** ： [用一段话总结整个章节]

---

**📍主题1： [标题] ** 
📝 **总结** ：……  
🚀 **说明** ：……  
💡 **论证** ：……

---

**📍主题2： [标题] ** 
📝 **总结** ：……  
🚀 **说明** ：……  
💡 **论证** ：……

（以此类推，直到最后一个段落）

**最终总结**  
在所有段落分析结束后，额外添加一段「全章核心脉络总结」（不超过150字），提炼整章最核心的主线和作者最终想传达的洞见。`,
  translate: `请将用户提供的文本内容进行中英文对照翻译，并严格遵循以下要求：

逐句处理：以句号、问号、感叹号等完整句子分隔符为单位，将原文拆分为独立句子进行翻译。
对照格式：每句话按"英文原文 换行 中文翻译"的格式呈现，确保双语对齐清晰。
保留原意：翻译需准确传达原文语义，避免过度意译或漏译，专业术语需统一。

特殊处理：
若原文为中英文混合内容，仅翻译非母语部分（如中文原文中的英文词汇保留不译）
保留数字、符号、专有名词（如人名、品牌名）原格式

输出示例：
The weather is lovely today.
今天天气真好。

How can I help you?
需要我帮忙吗？

响应规则
若输入为文档，请直接输出对照翻译结果
若输入仅含单语内容，按句子分段翻译
若输入格式混乱，先进行句子规范化再翻译
请确认要求后，回复"请提供需要翻译的内容"以开始流程。`,
  mindmap: `根据用户的要求，使用 mermaid 生成 mindmap.

要求：
* 中文内容使用双引号包裹，例如: "中文内容"
* 内容中不能使用的符号: ( )
* 只返回 mermaid 的内容(markdown格式), 不要返回其他AI回答的文字内容`,
  other: `你是一个阅读助手，专门帮助用户理解和分析电子书中的内容，回答用户的问题。`,
};

// 意图对应的 temperature 值
// ref: https://api-docs.deepseek.com/zh-cn/quick_start/parameter_settings
// temperature 参数默认为 1.0。
// 场景	温度
// 代码生成/数学解题   	0.0
// 数据抽取/分析	1.0
// 通用对话	1.3
// 翻译	1.3
// 创意类写作/诗歌创作	1.5
export const INTENT_TEMPERATURES: Record<Intent, number> = {
  summarize: 1.0,
  translate: 1.3,
  mindmap: 1.3,
  other: 1.3,
};

// 生成对话标题的 prompt
export const TITLE_PROMPT = `你是一个阅读助手。请根据用户的第一条提问，为这个对话生成一个简洁的中文标题（不超过20个字）。

要求：
1. 标题要能准确概括用户提问的主题
2. 使用简洁的中文
3. 不需要包含标点符号
4. 直接返回标题，不要有任何解释`;

// 压缩文本的 prompt 模板
export function buildCompressPrompt(content: string, highlights?: string[]): string {
  const highlightText = highlights && highlights.length > 0
    ? `\n\n用户重点关注的内容：\n${highlights.join('\n')}`
    : '';

  return `请对以下文本进行压缩精简，缩短篇幅，保留核心内容。重点关注用户选中的相关内容。${highlightText}

原文：
${content}

请直接输出压缩后的内容，不需要任何额外说明或格式。`;
}
